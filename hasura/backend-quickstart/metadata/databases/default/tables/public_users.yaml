table:
  name: users
  schema: public
array_relationships:
  - name: assigned_user_roles
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: assigned_user_roles
          schema: public
